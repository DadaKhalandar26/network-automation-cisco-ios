! --- Configure Timezone ---
clock timezone {{ timezone_name }} {{ timezone_offset_hours }} {{ timezone_offset_minutes }}

! --- Configure NTP Source ---
{% if ntp_source_interface is defined %}
ntp source {{ source_interface }}
{% endif %}

! --- Configure NTP Servers ---
{% for server in ntp_servers %}
ntp server {{ server }}
{% endfor %}

! --- Optional: Sync Hardware Clock (Calendar) ---
! (This command updates the BIOS clock)
ntp update-calendar
