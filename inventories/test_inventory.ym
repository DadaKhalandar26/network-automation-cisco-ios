---
- name: Test inventory variable resolution
  hosts: platforms_cisco-ios
  gather_facts: no
  tasks:
    - name: Show resolved variables for each host
      debug:
        var: ansible_connection

    - name: Show network OS plugin
      debug:
        var: ansible_network_os
