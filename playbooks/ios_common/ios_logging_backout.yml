---
- name: IOS | LOGGING BACK-OUT
  hosts: platforms_cisco-ios
  gather_facts: no

  tasks:
    - name: Back-out logging configuration
      import_role:
        name: ios_common
        tasks_from: logging/backout.yml
