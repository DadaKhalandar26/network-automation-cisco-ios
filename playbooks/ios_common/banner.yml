#Test Working
---
- name: Deploy login banner to Cisco IOS
  hosts: Test-IOS-XE-ISRV
  gather_facts: no
  
  vars:
    ansible_connection: network_cli
    ansible_network_os: cisco.ios.ios
  
  roles:
    - role: ios_common/banner
      vars:
        banner_file: banner.j2
        commit: true
